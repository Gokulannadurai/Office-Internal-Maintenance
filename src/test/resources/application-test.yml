spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/maintenance_test_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 0  # Random port for tests 